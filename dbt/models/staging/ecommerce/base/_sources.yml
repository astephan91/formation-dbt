version: 1

sources:
  - name: ecommerce
    schema: raw
    tables:
      - name: customers
        columns:
          - name: customer_id
            tests:
              - not_null
      - name: orders
        columns:
          - name: order_id
            tests:
              - unique
      - name: order_items